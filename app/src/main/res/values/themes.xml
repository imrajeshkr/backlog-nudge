<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.BacklogNudge" parent="android:Theme.Material.Light.NoActionBar">
        <!-- No brand-coloured status bar: it matches the page. Compose re-applies
             the exact background per light/dark at runtime (see BacklogNudgeTheme). -->
        <item name="android:statusBarColor">@color/bg_paper</item>
        <item name="android:navigationBarColor">@color/bg_paper</item>
        <item name="android:windowBackground">@color/bg_paper</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>

    <style name="Theme.BacklogNudge.Transparent" parent="Theme.BacklogNudge">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>
</resources>
